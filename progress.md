2026-02-28: Completed T:1 scaffold. Go module initialized (`github.com/vossenwout/claw-radio`) with Cobra dependency, required directory tree and stub files created, and structural acceptance tests added in `project_structure_test.go` (validated with `go mod tidy`, `go test ./...`, `go build ./...`).
2026-02-28: Completed T:2 config loader in `internal/config` with defaults, env override (`CLAW_RADIO_CONFIG`), `~` path expansion, binary auto-resolution (`exec.LookPath`), OS-conditional TTS fallback selection, and acceptance tests (`go test ./internal/config/...`).
2026-02-28: Completed T:3 mpv IPC client in `internal/mpv` with Unix socket request/response routing, async event stream channel, `InsertNext` queue placement logic, and `WaitForSocket` polling; acceptance tests were added and verified with `go test ./internal/mpv/...` (and full suite `go test ./...`).
2026-02-28: Completed T:4 yt-dlp wrapper in `internal/ytdlp` with command wrappers (`Search`, `BestCandidate`, `ResolveURL`, `Download`), dj-audio score port, and normalization; acceptance tests added for scoring thresholds, best-candidate selection from mocked ndjson output, and `NormalizeSongKey` (`go test ./internal/ytdlp/...`).
2026-02-28: Completed T:5 in `internal/station` and `cmd/station.go`. Implemented station/state load-save (atomic writes), seed round-robin + dedupe, played-song tracking, controller daemon loop (mpv reconnect backoff, end-file + 30s refill, cache GC, SIGTERM save), and hidden `claw-radio station daemon` command. Added acceptance tests in `internal/station/station_test.go`; verified with `go test ./internal/station/...`.
2026-02-28: Completed T:6 in `internal/provider`. Implemented `YtDlpProvider.Resolve` with YouTube search query (`ytsearch10:<seed> official audio`), cache hit short-circuit (`cacheDir/<video_id>.opus`), direct URL fast-path, and download fallback; added Spotify/AppleMusic stub providers returning "not implemented". Added acceptance tests in `internal/provider/provider_test.go` with a mocked `yt-dlp` binary; verified via `go test ./internal/provider/...` and `go test ./...`.
2026-02-28: Completed T:12 in `main.go` and `cmd/root.go`. Implemented root help behavior, version command (`claw-radio dev` default), coded exit-error helper flow, and main-process exit-code handling. Added acceptance tests in `cmd/root_test.go` and `main_test.go`; verified with `go test ./cmd -run 'TestExecute|TestExitCode'`, `go test . -run TestMainExitsWithCommandExitCode`, and `go test ./...`.
2026-02-28: Completed T:7 in `internal/search`. Implemented `Wikitable`, `Discogs`, `MusicBrainz`, and `Generic` extractors plus fixture-backed acceptance tests in `internal/search/search_test.go` and HTML fixtures under `internal/search/testdata/`; verified with `go test ./internal/search/... -run TestExtract` and `go test ./internal/search/...`.
