2026-02-28: Completed T:1 scaffold. Go module initialized (`github.com/vossenwout/claw-radio`) with Cobra dependency, required directory tree and stub files created, and structural acceptance tests added in `project_structure_test.go` (validated with `go mod tidy`, `go test ./...`, `go build ./...`).
2026-02-28: Completed T:2 config loader in `internal/config` with defaults, env override (`CLAW_RADIO_CONFIG`), `~` path expansion, binary auto-resolution (`exec.LookPath`), OS-conditional TTS fallback selection, and acceptance tests (`go test ./internal/config/...`).
2026-02-28: Completed T:3 mpv IPC client in `internal/mpv` with Unix socket request/response routing, async event stream channel, `InsertNext` queue placement logic, and `WaitForSocket` polling; acceptance tests were added and verified with `go test ./internal/mpv/...` (and full suite `go test ./...`).
2026-02-28: Completed T:4 yt-dlp wrapper in `internal/ytdlp` with command wrappers (`Search`, `BestCandidate`, `ResolveURL`, `Download`), dj-audio score port, and normalization; acceptance tests added for scoring thresholds, best-candidate selection from mocked ndjson output, and `NormalizeSongKey` (`go test ./internal/ytdlp/...`).
2026-02-28: Completed T:5 in `internal/station` and `cmd/station.go`. Implemented station/state load-save (atomic writes), seed round-robin + dedupe, played-song tracking, controller daemon loop (mpv reconnect backoff, end-file + 30s refill, cache GC, SIGTERM save), and hidden `claw-radio station daemon` command. Added acceptance tests in `internal/station/station_test.go`; verified with `go test ./internal/station/...`.
2026-02-28: Completed T:6 in `internal/provider`. Implemented `YtDlpProvider.Resolve` with YouTube search query (`ytsearch10:<seed> official audio`), cache hit short-circuit (`cacheDir/<video_id>.opus`), direct URL fast-path, and download fallback; added Spotify/AppleMusic stub providers returning "not implemented". Added acceptance tests in `internal/provider/provider_test.go` with a mocked `yt-dlp` binary; verified via `go test ./internal/provider/...` and `go test ./...`.
2026-02-28: Completed T:12 in `main.go` and `cmd/root.go`. Implemented root help behavior, version command (`claw-radio dev` default), coded exit-error helper flow, and main-process exit-code handling. Added acceptance tests in `cmd/root_test.go` and `main_test.go`; verified with `go test ./cmd -run 'TestExecute|TestExitCode'`, `go test . -run TestMainExitsWithCommandExitCode`, and `go test ./...`.
2026-02-28: Completed T:7 in `internal/search`. Implemented `Wikitable`, `Discogs`, `MusicBrainz`, and `Generic` extractors plus fixture-backed acceptance tests in `internal/search/search_test.go` and HTML fixtures under `internal/search/testdata/`; verified with `go test ./internal/search/... -run TestExtract` and `go test ./internal/search/...`.
2026-02-28: Completed T:8 in `internal/search/search.go` with 30s-timeout SearxNG client, URL fetch (`/search?q=...&format=json`), domain-based extractor dispatch, dedupe by lowercase `artist||title`, result cap, and silent per-page fetch skips. Added acceptance tests in `internal/search/search_client_test.go`; verified with `go test ./internal/search/...` and `go test ./...`.
2026-02-28: Completed T:9 in `internal/tts`. Implemented `Client.Render` three-layer fallback chain (warm daemon -> one-shot venv -> system TTS), optional `voice_prompt` request field behavior, and subprocess execution via package-level `execCommand`. Added acceptance tests in `internal/tts/tts_test.go`; verified with `go test ./internal/tts/...` and `go test ./...`.
2026-02-28: Completed T:10 in `tts/daemon.py` with warm daemon mode (Unix socket JSON protocol), optional `voice_prompt` support, cross-platform device detection (`mps`/`cuda`/`cpu`), one-shot mode (`--one-shot <text> <out_path> [--voice]`), and SIGTERM/SIGINT clean shutdown with socket cleanup. Added acceptance tests in `tts/daemon_test.py` using mocked `chatterbox`/`torch`/`torchaudio` modules (`python3 -m unittest tts/daemon_test.py`), then verified full Go suite still passes (`go test ./...`).
2026-02-28: Completed T:11 by adding `go:embed` wiring in `embed.go` (`ttsFS` embeds `tts/`), adding default bundled voice assets (`tts/voices/pop.wav`, `country.wav`, `electronic.wav`) plus `tts/voices/README.md`, and adding acceptance tests in `embed_test.go` to verify embedded `tts/daemon.py` is non-empty and `tts/voices/pop.wav` starts with `RIFF`. Verified with `go test ./...` and `go build ./...`.
2026-02-28: Completed T:13 in `cmd/start.go` and `cmd/stop.go`. Added dependency checks with install-hint exit code 4, stale PID cleanup, mpv/controller startup + PID writes, mpv socket timeout handling (exit 1), optional TTS daemon launch, and graceful stop cleanup (signals + PID/socket removal). Added acceptance tests in `cmd/start_stop_test.go`; verified with `go test ./cmd -run 'TestStart|TestStop|TestExecute'` and `go test ./...`.
2026-02-28: Completed T:14 in `cmd/play.go` with `play`, `queue`, `pause`, `resume`, and `next` commands, mpv preflight (exit 5 with start hint), and provider-based track resolution for play/queue. Added acceptance tests in `cmd/play_test.go` using a mock Unix mpv socket to verify command sequencing and property updates; verified with `go test ./cmd/...` and `go test ./...`.
2026-02-28: Completed T:15 in `cmd/seed.go` with JSON-array parsing, `--label` replacement mode, `--append` dedupe mode, station load/save, parse-failure exit code 1, and no-arg usage exit code 2. Added acceptance tests in `cmd/seed_test.go`; verified with `go test ./cmd -run Seed`, `go test ./cmd/...`, and `go test ./...`.
2026-02-28: Completed T:16 in `cmd/search.go` by adding `claw-radio search <query>` with JSON stdout formatting (`"Artist - Title"`), stderr fetch summary, and exit code 1 on SearxNG-unreachable errors. Added acceptance tests in `cmd/search_test.go` using `httptest` mock SearxNG/pages, and added `internal/search.Client.SearchWithStats` to report fetched-page count. Verified with `go test ./cmd -run Search` and `go test ./...`.
2026-02-28: Completed T:17 in `cmd/say.go` with mpv-required preflight (exit 5), voice resolution (`cfg.TTS.Voices` -> `data_dir/voices/<name>.wav` -> literal path -> default), banter file creation under `<data_dir>/banter/<ns>.wav`, TTS render handling (exit 4 on unavailable), mpv `InsertNext`, and success output (`queued banter`). Added acceptance tests in `cmd/say_test.go`; verified with `go test ./cmd -run Say`, `go test ./cmd/...`, and `go test ./...`.
2026-02-28: Completed T:19 in `cmd/events.go` with `events [--json]` command, mpv preflight exit code 5, mpv-event translation (`file-loaded` -> `track_started`, `end-file` -> `track_ended` + `queue_low`, channel close -> `engine_stopped`), and explicit per-line flush behavior. Added acceptance tests in `cmd/events_test.go` including a streaming flush test; verified with `go test ./cmd -run Events` and `go test ./...`.
2026-02-28: Completed T:18 in `cmd/tts.go` and `cmd/tts_test.go`. Implemented `claw-radio tts install` (python preflight, venv creation, embedded `daemon.py` + `tts/voices/*` extraction, pip install, CUDA warning) and `claw-radio tts voice add` (yt-dlp/ffmpeg dependency checks with exit 4 hints, voice download, 30s trim, confirmation output). Added embed wiring via `cmd.SetEmbeddedTTSFS(...)` in `main.go`. Verified with `go test ./cmd -run TestTTS` and `go test ./...`.
2026-02-28: Completed T:20 in `cmd/status.go` and `cmd/status_test.go`. Implemented `status [--json]` snapshot with best-effort engine/controller PID liveness, station seed summary from `station.json`, mpv playback/queue probing, and TTS mode detection (`warm`/`system`/`unavailable`) plus human-readable output. Added acceptance tests for all six criteria and verified with `go test ./cmd -run Status -count=1` and `go test ./...`.
2026-02-28: Completed T:21 by adding root `config.example.json` matching plan defaults and adding acceptance tests in `internal/config/config_example_test.go` (env-path load, top-level keys, empty binary fields, default queue depth + SearxNG URL); verified with `go test ./internal/config/...`.
2026-02-28: Completed T:22 by adding `.github/workflows/ci.yml` (push/PR on `main`, checkout/setup-go, gofmt check, vet, tests) and adding `ci_workflow_test.go` to validate workflow requirements plus gofmt-failure behavior. Fixed pre-existing Go 1.22 vet blockers in tests (`internal/config/config_example_test.go`, `cmd/status_test.go`, `cmd/events_test.go`) so CI checks pass; verified with `go vet ./...` and `go test ./...`.
